class Page {
  AUTH = "/auth";
  BASE = "/dashboard";

  MESSAGE = `${this.BASE}/message`;
  INSIGHT = `${this.BASE}/insight`;
  TEAM = `${this.BASE}/team`;
  SCHEDULE = `${this.BASE}/schedule`;
  REPORT = `${this.BASE}/report`;
  SETTINGS = `${this.BASE}/settings`;
}

export const PAGE = new Page();
